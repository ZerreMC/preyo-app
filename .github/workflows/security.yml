# Destino: .github/workflows/security.yml  (nuevo)
# Correcciones vs propuesta: comillas simples en la expresión (== 'pull_request'), Node 24.
# dependency-review va non-blocking porque en repos privados sin GitHub Advanced Security
# puede no estar disponible; quítale continue-on-error cuando el repo sea público o tengas GHAS.
name: Security

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"   # lunes 06:00 UTC

permissions:
  contents: read

jobs:
  audit:
    name: audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: pnpm audit
        run: pnpm audit --audit-level=moderate
        continue-on-error: true

  dependency-review:
    name: dependency-review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v5
      - uses: actions/dependency-review-action@v4
        continue-on-error: true
        with:
          fail-on-severity: moderate
          deny-licenses: GPL-3.0, AGPL-3.0
